apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: pc-edge
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3000
      targetPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: api-gateway-preview
  namespace: pc-edge
spec:
  selector:
    app: api-gateway
  ports:
    - port: 3000
      targetPort: 3000