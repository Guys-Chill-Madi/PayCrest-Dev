apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: manager-pvc
  namespace: pc-app
spec:
  storageClassName: pay-sc
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "2Gi"