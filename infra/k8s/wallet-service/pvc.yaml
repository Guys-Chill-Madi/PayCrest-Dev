apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallet-pvc
  namespace: pc-app
spec:
  storageClassName: nfs-csi1
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "2Gi"