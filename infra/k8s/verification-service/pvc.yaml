apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: wallet-pvc
  namespace: pc-app1
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "2Gi"