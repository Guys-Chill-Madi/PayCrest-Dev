apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: auth-pvc
  namespace: pc-app
spec:
  storageClassName: nfs-csi
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi 