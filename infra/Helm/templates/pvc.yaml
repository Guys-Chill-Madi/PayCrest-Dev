apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pc-upload-pvc
  namespace: pc-app
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  resources: 
    requests:
      storage: 10Gi