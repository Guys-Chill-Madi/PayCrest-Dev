apiVersion: v1 
kind: Service 
metadata:
  name: wallet-service 
  namespace: pc-app
spec:
  selector:
    app: wallet-service  
  ports:
    - targetPort: 8000
      port: 8000
  type: ClusterIP
    
  

