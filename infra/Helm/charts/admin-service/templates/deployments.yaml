apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-service
  namespace: pc-app
  labels:
    app: admin-service
    {{- include "paycrest.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.adminService.replicas }}
  selector:
    matchLabels:
      app: admin-service
  {{- include "paycrest.rollingUpdate" . | nindent 2 }}
  template:
    metadata:
      labels:
        app: admin-service
    spec:
      {{- include "paycrest.antiAffinity" "admin-service" | nindent 6 }}
      containers:
        - name: admin-service
          image: {{ .Values.adminService.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.adminService.resources | nindent 12 }}
          envFrom:
            {{- include "paycrest.appEnvFrom" . | nindent 12 }}
          volumeMounts:
            {{- include "paycrest.uploadVolumeMount" . | nindent 12 }}
          {{- include "paycrest.appProbes" . | nindent 10 }}
      volumes:
        {{- include "paycrest.uploadVolume" . | nindent 8 }}