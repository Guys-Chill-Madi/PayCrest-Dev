apiVersion: apps/v1
kind: Deployment
metadata:
  name: loan-service
  namespace: pc-app
  labels:
    app: loan-service
    {{- include "paycrest.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      app: loan-service
  {{- include "paycrest.rollingUpdate" . | nindent 2 }}
  template:
    metadata:
      labels:
        app: loan-service
    spec:
      {{- include "paycrest.antiAffinity" "loan-service" | nindent 6 }}
      containers:
        - name: loan-container
          image: {{ .Values.loanService.image }}
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          resources:
            {{- toYaml .Values.loanService.resources | nindent 12 }}
          envFrom:
            {{- include "paycrest.appEnvFrom" . | nindent 12 }}
          volumeMounts:
            {{- include "paycrest.uploadVolumeMount" . | nindent 12 }}
          {{- include "paycrest.appProbes" . | nindent 10 }}
      volumes:
        {{- include "paycrest.uploadVolume" . | nindent 8 }}