apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: pc-app
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: payment-service-preview
  namespace: pc-app
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
    - port: 8000
      targetPort: 8000