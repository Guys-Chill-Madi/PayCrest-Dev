apiVersion: v1
kind: Service
metadata:
  name: verification-service
  namespace: pc-app
spec:
  type: ClusterIP
  selector:
    app: verification-service
  ports:
    - port: 8000
      targetPort: 8000