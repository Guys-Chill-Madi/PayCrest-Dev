apiVersion: v1
kind: Service
metadata:
  name: emi-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: emi-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}