apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: pc-app
spec:
  selector:
    app: admin-service
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service-preview
  namespace: pc-app
spec:
  selector:
    app: admin-service
  ports:
    - port: 8000
      targetPort: 8000