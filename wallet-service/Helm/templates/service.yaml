apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: pc-app
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
    - port: 8000
      targetPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: wallet-service-preview
  namespace: pc-app
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
    - port: 8000
      targetPort: 8000